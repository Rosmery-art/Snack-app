import React from 'react';
import { StyleSheet, View, Text, Image, ImageBackground, TouchableOpacity } from 'react-native';
import { useNavigation } from '@react-navigation/native';

export default function Registro() {
  const navigation = useNavigation();

  return (
    <ImageBackground
      source={require('./assets/perfil.png')} // Asegúrate de que la ruta sea correcta
      style={styles.background}
      resizeMode="cover"
    >
      <View style={styles.container}>
        {/* Título */}
        <Text style={styles.title}>USTED HA ESCOGIDO NO TOMAR EL REFRIGERIO ESCOLAR EL DÍA DE HOY</Text>
        <Text style={styles.title}>NO PUEDE SALIR</Text>
        </View>

    </ImageBackground>
  );
}

const styles = StyleSheet.create({
  background: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  container: {
    flex: 1,
    justifyContent: 'flex-start', // Mantener los elementos en la parte superior
    alignItems: 'center',
    padding: 5,
  },
  title: {
    textAlign: 'center',
    fontSize: 35,
    fontWeight: 'bold',
    color: '#FFFFFF',
    marginTop: 140, // Espacio en la parte superior
    marginLeft:40,
    marginRight:40,
  },
});
